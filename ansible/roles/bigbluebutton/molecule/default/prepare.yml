---

- name: Disk size
  hosts: all
  become: true
  tasks:
    - name: Disk usage from command module
      command: df -h
      register: df
    - name: Print df
      debug:
        var: df.stdout_lines
    - name: Install dependencies
      apt:
      cache_valid_time: 60
        name: gnupg2
