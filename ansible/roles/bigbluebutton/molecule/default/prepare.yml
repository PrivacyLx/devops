---

- name: Prepare
  hosts: all
  become: True
  tasks:
    - name: Install PyOpenSSL build requirements
      apt:
        name: ['python3-pip', 'python3-setuptools']
        update_cache: yes
    - name: pip self-update
      pip:
        name: ['pip', 'wheel']
        state: latest
        executable: pip3
